# @Grep

The `grep` command helps us find all described or used details.
Here is a list of options we use for this:

-E -- use extended regular expressions

-I -- only process text files

-h -- don't print filenames

-r -- search for files recursively

-o -- print only matches, not the whole line
