# @TmpDir

We use the temporary directory `/tmp/details` to save two lists -
described details and used details and then find the differences between them.

Temporary files in Unix are cleaned up periodically and we don't care
about deleting the created files.
