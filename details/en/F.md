# @FunctionsInBash

Functions in Bash allow you not to move the functionality into a separate utility,
but to use it several times if necessary. Usually functions are used inside
the same script in which they are declared.
